# Automated Unit Testing with GitHub Actions

This project implements basic arithmetic functions such as add() and sub() and validates them using pytest unit tests. A GitHub Actions CI pipeline automatically triggers on every push or pull request to the main branch, sets up a Python environment, installs dependencies and runs tests to ensure code correctness and prevent integration issues.

